version: '3'

services:

  backend:
    image: crud-backend
    environment:
      DB_USER: admin
      DB_PASS: Cloud123
      DB_HOST: database-1.cxaeu48q49rz.us-east-1.rds.amazonaws.com
      DB_NAME: cruddb
    ports:
      - "5000:5000"

  frontend:
    image: crud-nginx
    ports:
      - "80:80"
    depends_on:
      - backend
