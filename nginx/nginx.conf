events {}

http {
    server {
        listen 80;

        # Serve frontend static files
        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        # Route API calls to backend container
        location /api/ {
            proxy_pass http://backend:5000/;
        }
    }
}
